# 追加ヒアリングシート: 優先論点

作成日時: 2026-03-05 01:23:34 +09:00
作成者: Codex + GPT-5

## 1. 使い方

- `未定`、`対象外`、`分からない` でも問題ありません
- 数値が未確定の場合は、`だいたい` や `上限想定` でも構いません
- 回答後は、要件定義、設計書、実装着手条件へ正式反映する前提です

---

## 2. このシートで確認したいこと

今回の追加ヒアリングは、以下の高優先項目だけを対象にしています。

1. 送付元マスタでどこまで業務を吸収できるか
2. 添付ファイルや返信メールの容量をどう扱うか
3. 正式指示値に対して、どこまで承認や証跡が必要か
4. 代理送信、転送、重複送信などの実務上の癖があるか
5. 保存期間や BCP の考え方をどこまで厳しくするか

---

## 3. 優先論点ヒアリング

### 3.1 送付元マスタの実態

1. 現時点で、送付元メールアドレスと案件の対応表はどの程度整備できそうですか。  
回答:

2. `500人` の利用者のうち、初期 PoC や先行導入の対象にするメールアドレス数はどの程度ですか。  
回答:

3. 送付元メールアドレスが途中で変わる、または複数アドレスを使うケースはありますか。  
回答:

4. 協力会社、代理送信、共用アドレスの利用はありますか。ある場合、どの程度ありますか。  
回答:

5. 送付元マスタの整備率が低い場合、`確認待ち` に回す運用で許容できますか。それとも先にマスタ整備を優先すべきですか。  
回答:

### 3.2 メールの実務上の癖

1. 同じ案件について、同じ PDF を再送することはありますか。  
回答:

2. メールの転送で依頼されることはありますか。  
回答:

3. `件名だけ変えて再送`、`本文だけ変えて再送` のようなケースはありますか。  
回答:

4. 重複送信を防ぐため、`同一送信元 + 同一添付ファイル` を一定時間内は重複判定する運用にしても問題ありませんか。  
回答:

5. 自動返信ループを避けるため、システムからの返信に再返信しても再処理しない制御は必要ですか。  
回答:

### 3.3 添付ファイルと返信容量

1. `最大10KB` の回答は再確認したいです。実際の仕様票 PDF のサイズ感はどれくらいですか。  
回答:

2. 1件あたりの平均ページ数、最大ページ数の感覚値はありますか。  
回答:

3. 返信時に添付する `結果帳票 + マニュアル + 図面` の合計容量は、どの程度になりそうですか。  
回答:

4. 返信メールが大きすぎる場合、`添付ではなくSharePointリンクを返す` 方式は許容できますか。  
回答:

5. 返信メールを `分割送信` することは許容できますか。  
回答:

### 3.4 正式指示値としての承認と証跡

1. 正式指示値を返す際、`人の承認なしで自動返信してよい条件` はありますか。  
回答:

2. 逆に、`必ず人が確認してから返すべき条件` はありますか。  
回答:

3. 承認記録として、最低限どこまで残す必要がありますか。  
回答:

4. 計算結果を差し戻した場合、差戻理由の記録は必要ですか。  
回答:

5. 将来的に監査や事故調査が発生した場合、`誰が、どの版で、どの条件で返したか` が追えれば十分ですか。  
回答:

### 3.5 保存期間と事故調査

1. `3か月保存` で、現実の問い合わせや事故調査に足りますか。  
回答:

2. もし不足する場合、`6か月`、`1年`、`案件完了まで` のどれに近いですか。  
回答:

3. 添付 PDF 本体は短期保存、月次転記した集計だけ長期保存、のような分け方は許容できますか。  
回答:

4. 事故調査で一番見返したいのは何ですか。`原本PDF`、`入力値`、`結果`、`返信内容`、`使用版`。  
回答:

### 3.6 BCP と障害時継続

1. M365 障害時に、`どの時点で手動運用へ切り替えるか` の目安はありますか。  
回答:

2. 手動運用へ切り替える判断者は誰ですか。  
回答:

3. 手動運用時に使う Excel やマクロ、バッチの保管場所は決まっていますか。  
回答:

4. 障害時に現場へは、`遅延連絡`、`通常どおり手動返信`、`社内だけで吸収` のどれが望ましいですか。  
回答:

5. BCP の観点で、本番前に最低限テストしておきたい障害ケースは何ですか。  
回答:

### 3.7 マスタ更新と誤更新防止

1. 送付元案件マスタ、資料マスタ、Excel 正本の更新は、`更新者` と `確認者` を分けた方がよいですか。  
回答:

2. マスタ更新を即日反映してよいですか。それとも有効開始日で管理すべきですか。  
回答:

3. 誤更新があった時に、直前版へ戻せれば十分ですか。  
回答:

4. 更新時に通知したい相手は誰ですか。  
回答:

5. 将来 10機能、30機能へ増えることを考えたとき、`ルール台帳` を作ることは必要だと思いますか。  
回答:

---

## 4. 補足メモ

- このシートは、`08_追加検討論点_最終版.md` で挙げた高優先論点を、回答しやすい形へ落としたものです
- 回答後は、要件定義書の未確定事項、設計書の実装前決定事項、詳細設計書の運用条件へ反映します
